{
  "name": "fitness-realtors",
  "version": "1.0.0",
  "description": "Fitness Realtors authentication system with Cloudflare Workers and D1",
  "main": "worker.js",
  "scripts": {
    "sync:public": "powershell -ExecutionPolicy Bypass -File ./sync-public.ps1",
    "deploy": "npm run sync:public && wrangler deploy",
    "dev": "npm run sync:public && wrangler dev"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "wrangler": "^4.74.0"
  }
}
