{
  "name": "commission-calculator-pages",
  "version": "1.0.0",
  "description": "Commission Calculator deployed on Cloudflare Pages with D1-backed auth",
  "main": "public/_worker.js",
  "scripts": {
    "deploy": "wrangler pages deploy public --project-name=commission-calculator",
    "dev": "wrangler pages dev public"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "wrangler": "^4.74.0"
  }
}
